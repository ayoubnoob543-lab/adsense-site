# AdSense Site

Sitio web creado con Vite + React que incluye el codigo de Google AdSense en el `<head>`.

## Como usar

```bash
npm install
npm run dev
```

El codigo de AdSense ya esta integrado en `index.html` dentro de las etiquetas `<head>`.
